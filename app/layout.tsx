import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Launchpad - Donor Connection Platform",
  description: "Launchpad's mission is to build accelerated pathways, connecting high school students to high-paying, future-ready careers. Connect with donors and see the impact of contributions.",
  keywords: ["donor", "philanthropy", "Philadelphia", "charity", "community", "Launchpad", "tech careers", "education", "workforce development"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
